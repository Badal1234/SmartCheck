var web3 = require('web3')
var solc = require('solc');
module.exports.convertBytecode = (filePath,language) => {
    var input = {
        language: language,
        sources:{
            
        }
    }

}